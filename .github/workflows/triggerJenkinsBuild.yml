name: trigger jenkins job
on:
  push:
    branches:
      - main
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: trigger single Job
      uses: appleboy/jenkins-action@master
      with:
        url: "https://1c529d9b81b8.ngrok.io"
        user: "vacahagan"
        token: ${{ secrets.TOKEN }}
        job: "testing"
